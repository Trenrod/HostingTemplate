import { describe } from "@jest/globals";

/**
 * Generates Inventory file for the given configuration
 */
describe("applyCheck", () => {
	it("Execute check action", () => {
		expect(true).toBe(true);
	})
});